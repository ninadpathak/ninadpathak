# Configuration reference

Reference pages define stable names, types, defaults, and constraints. They should not hide the first setup path.

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `EXAMPLE_API_TOKEN` | string | Yes | Test token used by the example requests. |
| `EXAMPLE_API_URL` | URL | No | API base URL. Defaults to the production endpoint. |
