# Brief: Automated API Documentation From OpenAPI

**Slot:** 2026-09-21 | Order 54 | **Cluster:** Documentation | **Experience: B**

## Keyword

`automated api documentation` measures 30/mo. Near zero, and `generate openapi spec from code`
(150 / KD 6, own parent topic) is the better target inside the same piece. Lead with the generation
question rather than the phrase.

## Reader task

Generate reference from a spec while keeping the human-written guidance that generation cannot
produce.

## Owns

The generation boundary: what a spec can produce and what it structurally cannot.

## Must not repeat

**Four API documentation articles are live.** The tools comparison owns tool choice and already
covers OpenAPI-first workflows. The best-practices anchor owns the docs system. This page owns
generation mechanics only, and if it drifts into comparing generators it has become the live tools
page.

## Evidence — Experience B

Run a generator against a real OpenAPI spec and show the output beside what a reader still needs.
The finding worth having is the gap: generated reference is complete and unusable on its own, and
naming exactly which parts are missing is the article.

**Title honesty:** if no generator is actually run, the title overpromises and must change.

## Internal links

Outbound:
- `/articles/api-documentation-tools-hands-on-comparison-small-teams/`
- `/articles/api-documentation-best-practices-reference-guides-and-working-requests/`

**Inbound retrofit source:** the tools comparison. It chooses the tool; this page shows what the
tool leaves undone.
