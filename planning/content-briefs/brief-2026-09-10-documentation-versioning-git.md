# Brief: Documentation Versioning in Git

**Slot:** 2026-09-10 | Order 43 | **Cluster:** Documentation | **Experience: B**

## Keyword

`documentation versioning` measures **10/mo**. Near zero. Completes the set; do not pad.

## Reader task

Align product versions, branches, and published documentation.

## Owns

The Git mechanics: branches, tags, and directory strategies for versioned docs.

## Must not repeat

**The live `how-to-document-multiple-product-versions` owns the reader-facing policy**: version
states, canonicals, redirects, and the route audit. This page owns the repository side only. If
the draft starts discussing canonical tags it has become the live page.

## Evidence — Experience B

Build a small versioned docs tree and show what each strategy costs at merge time. What could
surprise you is which strategy makes a backport painful, and that only shows up when you try it.

Do not present a strategy comparison as tested if it was reasoned rather than run.

## Internal links

Outbound:
- `/articles/how-to-document-multiple-product-versions/`
- row 31 `docs-as-code`

**Inbound retrofit source:** `/articles/how-to-document-multiple-product-versions/`, which owns
the policy and should delegate the repository mechanics.
