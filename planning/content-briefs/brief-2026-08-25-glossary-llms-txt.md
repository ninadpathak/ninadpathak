# Brief: Glossary term, llms.txt

**Slot:** 2026-08-25 | **Type:** GLOSSARY
**Target:** `content/data/glossary.yaml`, term `llms-txt`.

## What to write

`short_definition`, `long_definition`, and `description` on the existing entry.

## The one hard constraint

Do not claim that publishing the file guarantees inclusion, citation, or ranking in AI
answers. `documentation-authority-plan.md` sets this guardrail explicitly and it holds
here. State what the proposal specifies and what is actually known about adoption. If
support by a given assistant is unverified, say it is unverified.

This matters more than usual: the corpus audit found that unsupported claims were the
main reason eight review-status posts were graded Poor.

## Keyword data

`llms.txt` 3,100 vol KD 56. `what is llms.txt` 1,800 KD 48. Parent topic for both is
`llms.txt`, so one page owns the definitional cluster and this term supports it rather
than competing.

## Owns

The definition at glossary depth, and the honest limits.

## Must not repeat

File construction and validation, which the implementation guide owns. The full topic
treatment, which the 2026-08-26 anchor owns.

## Article links, already wired and verified

- `/articles/seo-for-technical-documentation/`
- `/articles/types-of-technical-documentation/`

## Note

The site already ships a working generator at `/llms-txt-generator/` and its own
`llms.txt` at the domain root. Both are legitimate evidence and can be referenced.
