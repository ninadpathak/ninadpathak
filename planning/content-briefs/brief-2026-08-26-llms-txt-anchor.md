# Brief: llms.txt, what it is and what it does not do

**Slot:** 2026-08-26 | **Type:** NEW (anchor) | **Proposed slug:** `llms-txt`

## This brief corrects the canonical plan

`documentation-authority-plan.md` makes `llms.txt generator` the Month 5 anchor at 2,400
volume, KD 35, sourced from Semrush. Ahrefs puts `llms.txt generator` at **300**.

Demand sits on the definitional term instead:

| Keyword | Volume | KD | Parent topic |
|---|---:|---:|---|
| llms.txt | 3,100 | 56 | llms.txt |
| what is llms.txt | 1,800 | 48 | llms.txt |
| llms.txt file | 500 | 51 | llms.txt |
| what is llms.txt file | 250 | 43 | llms.txt |
| what is an llms.txt | 200 | 39 | llms.txt |

Combined 6,200 on one parent topic, meaning one page can own all of it. The generator
stays a supporting asset. It already exists at `/llms-txt-generator/`.

KD 56 is the highest target in the 90 days and this will not rank quickly. It is worth the
slot because the topic is the site's clearest claim to the AI-ready documentation position.

## Reader task

Decide whether to publish an llms.txt file, and know what putting one up will and will not
do.

## Owns

The definitional cluster, the adoption picture, and the limits.

## Must not repeat

File construction and validation, which the implementation guide owns in weeks 9 to 11.
The generator tool, which `/llms-txt-generator/` owns. Crawler access rules, which the
robots.txt piece owns.

## Evidence required

Read real llms.txt files from live documentation sites, validate them against the
specification, and record the date of each check. Preserve the receipts.

State adoption honestly. Where an assistant's support is claimed but unverified, mark it
unverified rather than repeating a vendor claim.

This site's own root `llms.txt` is a legitimate example and is generated by `build.py`
from source, which is a point worth making concretely.

## The 53 questions with no SERP history

The Ahrefs pull returned 53 llms.txt keywords with real volume but null difficulty, null
parent topic, and no SERP features, totalling 8,760 monthly volume. Ahrefs has demand for
them and no ranking data, which means they are too new or too conversational for a stable
SERP. Their shape is the tell:

- should i create an llms.txt file?
- llms.txt go on site root or where?
- will llms.txt file help your seo
- what to do with llms.txt file
- how to see the llms.txt file of a website
- ai.txt vs llms.txt

These read as things typed into an assistant. **Do not make them 53 pages.** Use them as
this page's section headings and FAQ entries, each phrased as the question with the answer
immediately beneath it, self-contained. Full list in
`planning/research-cache/A5-matching-terms-llmstxt-phrase.json`.

## Internal links, all verified in the built sitemap

- `/llms-txt-generator/` the working tool
- `/articles/seo-for-technical-documentation/` for the crawl and index layer
- `/articles/types-of-technical-documentation/` for agent instructions as an audience

## Required

`faqs` frontmatter, drawn from the question list above. This is the page where FAQPage
schema matters most.
