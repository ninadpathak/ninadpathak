# Brief: Expand the technical documentation template page to own "examples"

**Slot:** 2026-08-19 | **Type:** EXPAND
**Target URL:** `/articles/technical-documentation-template/` (already published, do not create a new URL)

## Why this is an expansion and not a new article

Ahrefs reports `technical documentation example` at 350 volume, KD 2, with **parent topic
`technical documentation template`**. `documentation example` at 250 has parent topic
`technical documentation example`. Google already resolves this family to whichever page
owns the template query.

That page exists and is published. A separate examples article would compete with an asset
already held. This is the highest-return single move in the 90 days because it adds roughly
600 monthly volume to a page that already ranks instead of starting a new URL at zero.

It also overrides Release 10 in `documentation-authority-plan.md`, which scheduled a
standalone examples anchor.

## Keyword data

| Field | Value |
|---|---|
| Page's existing primary | technical documentation template, 500 vol, KD 0 |
| Absorbing | technical documentation example, 350 vol, KD 2 |
| Absorbing | documentation example, 250 vol, KD 8 |
| Parent topic | technical documentation template (all three) |
| Intent | Informational |
| AI Overview | Yes, on all three |

## Reader task

Take the template and see it filled in, so the reader can tell whether their own draft is
finished.

## Owns

The template artifact, the page inventory it produces, and completed worked examples of
each document type built from that template.

## Must not repeat

The definition of technical documentation, which `/articles/what-is-technical-documentation-and-what-should-it-include/`
owns. The eight-type taxonomy, which `/articles/types-of-technical-documentation/` owns.
The review criteria, which the best-practices page owns. Link to those instead.

## Evidence required

Build the examples from the template already published on this page, for one sample
product, so the reader sees the same structure before and after it is filled. Do not paste
screenshots of other companies' documentation as the examples. Each example must be a
complete document, not an excerpt.

## Internal links, all verified in the built sitemap

- `/articles/types-of-technical-documentation/` for choosing the format
- `/articles/technical-documentation-best-practices-tested-real-developer-docs/` for the review card
- `/articles/documentation-review-checklist-before-you-publish/` for the pre-publish pass

## On publish

Set `updated`. The AI Overview presence means the new example sections should each open
with a self-contained sentence naming what the example is, so a retrieved passage stands
alone.
