# Brief: Docs-as-Code Tools: The Minimum Stack You Actually Need

**Slot:** 2026-09-01 | Order 34 | **Cluster:** Documentation | **Experience: A**

## Keyword, with an honest caveat

| Field | Value |
|---|---|
| Primary | `docs as code tools` |
| Volume | **20/mo** (Semrush 2026-08-17) |

**This is a near-zero keyword and the brief says so up front.** It survives because it completes
the docs-as-code owner set built by rows 31 to 33 and because the evidence is genuinely his. Write
it for the reader who read row 31 and wants the stack, not for search volume. Do not pad it.

If the draft starts stretching to justify the keyword, that is the signal to cut it short. A
short, honest piece is the correct outcome here.

## Reader task

Choose the smallest set of tools that makes a Git-based documentation workflow work.

## Owns

The minimum stack, and what each piece is actually for.

## Must not repeat

The workflow itself (row 31). Platform choice (row 32). Maintenance cadence (row 33). Code
reference generators (row 30). This is narrow by design: which tools, and why each one earns a
place.

## Evidence — Experience A

This site's stack is the artifact and it is unusually honest because it is small: a Python
generator, a writing linter that gates publish, a build-time SEO audit, a link-retrofit gate, a
test suite, and Cloudflare Pages. Say what is in it, and say what is deliberately absent.

The strongest material is subtractive: which tools he considered and did not adopt, and why the
stack stayed small.

## Internal links

Outbound:
- row 31 `docs-as-code`, row 32, both live by now

**Inbound retrofit source:** row 31's `docs-as-code` anchor. It is the parent and should delegate
tooling.
